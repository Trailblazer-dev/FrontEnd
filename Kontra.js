let leftDown = false;
let rightDown = false;
let upDown = false;
let downDown = false;

document.addEventListener('keydown', event => {
 if (event.key === 'ArrowLeft') leftDown = true;
 if (event.key === 'ArrowRight') rightDown = true the "A" and "D" keys for left and right movement, respectively:

```javascript
 how to use Kontra.js in a webkontra.initKeys(['a', 'd']);

kont;
 if (event.key === 'ArrowUp') upDown project. For more detailed information, you can refer to the official documentation: https://ra.keyPressed('a', () => {
 player.x -= 5;
});

kontra.key = true;
 if (event.key === 'ArrowDown') downDown = true;
straker.github.io/kontra/docs/index.html.});

document.addEventListener('keyup', event => {Pressed('d', () => {
 player.x += 5;
 if (event.key
});